let val = [2, 4, 3, 6, 5]
//val.sort()
console.log(val)

for (pos = 0; pos < val.length; pos++ ) {
    console.log(`A posição ${pos} tem o valor ${val[pos]}`)
}
/* // forma simplificada para usar FOR com arrays
for (let pos in val) {
    console.log(`A posição ${pos} tem o valor ${val[pos]}`)
}
*/
